{
  "description": "Optional stop-time review gate via Codex in a tmux pane.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/stop-hook.mjs\"",
            "timeout": 900
          }
        ]
      }
    ]
  }
}
